/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/Portfolie",
    assetPrefix: "/Portfolie/",
    output: "export",
    reactStrictMode: true,
    
}; 

export default nextConfig;
